# The team moment

The v1.12 demo brainstorm now exits into a team. Same session, same register:
after the recap was blessed and the promote route chosen, the station read the
captured work shape and derived the roster in the sounding-board voice, 1
paragraph, 1 blessing move, every charter shown verbatim with its owns and
reviews surfaces. What follows is that paragraph as presented, then the machine
receipt the station earned before committing `TEAM.md`.

---

Here is the crew this book asks for, read off the page and nothing else. The
locked physics and the render-tax make canon drift the way this story dies, so
1 seat keeps the record: a lore keeper who alone writes the canon block, every
fact checkable with chapter:line provenance (owns `LORE.md`). The next step is
a drafted cold open and the shape is a full novel, so 1 seat writes prose: a
chapter drafter working against the planner's contract in the salt-rot
register (owns `book/chapters/**`). The 4 payoff threads and the countdown need
someone whose only job is to catch the break: a continuity checker who owns
nothing and can therefore fail anything (reviews `book/chapters/**` and
`LORE.md`). The tone section is a hard contract, so a line editor holds the
register sentence by sentence (reviews `book/chapters/**`). And 1 seat no
registry agent covers: a memory-economy specialist to work out the Ledger
notary mechanics, the custody stamps, the render-tax arithmetic, and the
forgery asymmetry as rules that cannot contradict themselves, chartered inline
at the frontier tier (owns `book/tech/**`, reviews `LORE.md` and
`book/chapters/**`). No sixth seat survives the non-redundancy test. Bless it,
swap a seat, or trim it to fewer; my recommendation is these 5.

Blessed as presented. [solo dogfood: self-answered]

---

The station floor, machine output, before the commit:

```
team-manifest/v1: 23/23 checks, 5 roles, 4 bound
```

`TEAM.md` in this directory is the real validated artifact, byte for byte the
manifest the receipt above was earned on (manifest_hash `2217ce11a063...`), 4
seats bound by reference to shipped agents and 1 carried inline, exactly the 2
rungs of the binding ladder.
