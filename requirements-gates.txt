# Python runtime for the gate library packs (v1.9 Wave 4+).
# Install into the operator's gate venv, never into the node distribution:
#   python3 -m venv ~/.cache/ferrox/gates/venv && ~/.cache/ferrox/gates/venv/bin/pip install -r requirements-gates.txt
#
# openpyxl is REQUIRED by gates/spreadsheets/gate.py and generators.py.
# formulas is the OPTIONAL headless recalc engine for SS-01/SS-03/SS-05 depth;
# it is EUPL-1.1 licensed (outside the shipped-pack allowlist), so it stays a
# host-side optional dependency and the gate degrades as documented without it.
openpyxl==3.1.5
formulas==1.3.4
